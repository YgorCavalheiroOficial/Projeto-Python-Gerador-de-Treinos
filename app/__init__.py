"""Pacote principal da aplicação FitLogic.

Organiza o sistema em camadas MVC: ``models`` (entidades de domínio e
persistência), ``controllers`` (regras de orquestração entre views e
models), ``services`` (regras de negócio reutilizáveis, como geração de
treino, exportação em PDF e internacionalização) e ``views`` (interface
gráfica construída com CustomTkinter).
"""
